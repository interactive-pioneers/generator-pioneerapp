{
  "name": "<%= appname %>",
  "private": true,
  "dependencies": {<% if (includeBootstrap && includeSass) { %>
    "bootstrap-sass": "~3.3.6"<% } else if (includeBootstrap) { %>
    "bootstrap": "~3.3.5"<% } else { %>
    "jquery": "~1.12.2"<% } %><% if (includeModernizr) { %>,
    "modernizr": "3.3.1"<% } %>
  },
  "devDependencies": {
  }
}
