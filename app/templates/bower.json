{
  "name": "<%= appname %>",
  "private": true,
  "dependencies": {<% if (includeBootstrap && includeSass) { %>
    "bootstrap-sass-official": "~3.2.0",<% } else if (includeBootstrap) { %>
    "bootstrap": "~3.2.0",<% } else { %>
    "jquery": "~1.11.1",<% } %><% if (includeModernizr) { %>,
    "modernizr": "~2.8.2"<% } %>
  },
  "devDependencies": {
  }
}
